//Truthy values:

let age = 25; //(any non-zero number is truthy)
let name = "John";// (any non-empty string is truthy)
let is = true;// (boolean true is truthy)
//Falsy values:

let num = 0; //(the number zero is falsy)
let emptyStr = ""; //(an empty string is falsy)
let x= false; //(boolean false is falsy)